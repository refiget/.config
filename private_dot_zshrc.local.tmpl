# Machine-local config generated by chezmoi
export MACHINE_NAME="{{ (splitList "." .chezmoi.hostname | first) }}"
export JUPYTER_NOTEBOOK_DIR="$HOME/ml/"

# Shared encrypted secrets
[[ -f ~/.zshrc.secrets ]] && source ~/.zshrc.secrets

echo "zsh local exported"
